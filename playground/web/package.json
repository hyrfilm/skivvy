{
  "name": "skivvy-playground-web",
  "private": true,
  "type": "module",
  "scripts": {
    "overlay:build": "node ./scripts/build-overlay.mjs",
    "predev": "npm run overlay:build",
    "prebuild": "npm run overlay:build",
    "prepreview": "npm run overlay:build",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "nanoterm": "^0.1.2"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
