{
  "_comment": "Data integrity: every single user has a valid-looking email — one test covers the whole collection",
  "url": "/users",
  "match_every_entry": true,
  "match_subsets": true,
  "response": [
    {
      "email": "$regexp (.+)@(.+)\\.(.+)"
    }
  ]
}
